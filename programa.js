console.log('=== Programa ===');

// Entrada de dados
const nome = prompt('Digite seu nome: ');

// Preocessamento de dados


// Saída de dados
document.write('Olá, ' + nome.repeat(5));
document.write(nome.toUpperCase());